<Window x:Class="DesktopPlus.TrayMenuWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="252"
        SizeToContent="Height"
        WindowStyle="None"
        ResizeMode="NoResize"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        Topmost="True"
        ShowActivated="True"
        Focusable="True">
    <Window.Resources>
        <LinearGradientBrush x:Key="TraySurface" StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="#252B36" Offset="0"/>
            <GradientStop Color="#1F242D" Offset="1"/>
        </LinearGradientBrush>
        <SolidColorBrush x:Key="TrayBorder" Color="#3C465A"/>
        <SolidColorBrush x:Key="TrayText" Color="#F2F5FA"/>
        <SolidColorBrush x:Key="TrayMuted" Color="#AAB6CA"/>
        <SolidColorBrush x:Key="TrayHeaderBadge" Color="#2C3647"/>
        <SolidColorBrush x:Key="TrayHeaderBadgeBorder" Color="#4A5E7E"/>
        <SolidColorBrush x:Key="TrayButtonBg" Color="#2A3345"/>
        <SolidColorBrush x:Key="TrayButtonBorder" Color="#4A5D7B"/>
        <SolidColorBrush x:Key="TrayButtonBgHover" Color="#34445E"/>
        <SolidColorBrush x:Key="TrayButtonBorderHover" Color="#6C88B2"/>
        <SolidColorBrush x:Key="TrayDangerBg" Color="#3A2D34"/>
        <SolidColorBrush x:Key="TrayDangerBorder" Color="#7E4C5D"/>
        <SolidColorBrush x:Key="TrayDangerBgHover" Color="#4A333D"/>
        <SolidColorBrush x:Key="TrayDangerBorderHover" Color="#C26A84"/>

        <Style x:Key="TrayActionButton" TargetType="Button">
            <Setter Property="Foreground" Value="{StaticResource TrayText}"/>
            <Setter Property="Background" Value="{StaticResource TrayButtonBg}"/>
            <Setter Property="BorderBrush" Value="{StaticResource TrayButtonBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="Margin" Value="0,0,0,6"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="10"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Stretch"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource TrayButtonBgHover}"/>
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource TrayButtonBorderHover}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#3F5270"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="TrayDangerButton" TargetType="Button" BasedOn="{StaticResource TrayActionButton}">
            <Setter Property="Background" Value="{StaticResource TrayDangerBg}"/>
            <Setter Property="BorderBrush" Value="{StaticResource TrayDangerBorder}"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource TrayDangerBgHover}"/>
                    <Setter Property="BorderBrush" Value="{StaticResource TrayDangerBorderHover}"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#5D3A46"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Border Background="{StaticResource TraySurface}"
            BorderBrush="{StaticResource TrayBorder}"
            BorderThickness="1"
            CornerRadius="14"
            Padding="12">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20"
                              ShadowDepth="0"
                              Opacity="0.36"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0"
                  Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Width="26"
                        Height="26"
                        CornerRadius="8"
                        Background="{StaticResource TrayHeaderBadge}"
                        BorderBrush="{StaticResource TrayHeaderBadgeBorder}"
                        BorderThickness="1"
                        VerticalAlignment="Top">
                    <Image x:Name="HeaderIcon"
                           Width="14"
                           Height="14"
                           RenderOptions.BitmapScalingMode="HighQuality"
                           VerticalAlignment="Center"
                           HorizontalAlignment="Center"/>
                </Border>

                <StackPanel Grid.Column="1"
                            Margin="10,0,0,0">
                    <TextBlock Text="Desktop+"
                               Foreground="{StaticResource TrayText}"
                               FontSize="13"
                               FontWeight="SemiBold"/>
                    <TextBlock x:Name="TrayStatusText"
                               Foreground="{StaticResource TrayMuted}"
                               FontSize="11"
                               Margin="0,2,0,0"/>
                </StackPanel>
            </Grid>

            <Border Grid.Row="1"
                    Height="1"
                    Margin="2,0,2,10"
                    Background="#2E3744"
                    Opacity="0.8"/>

            <StackPanel Grid.Row="2">
                <Button x:Name="OpenMainButton"
                        Style="{StaticResource TrayActionButton}"
                        Click="OpenMainButton_Click">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="28"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="20"/>
                        </Grid.ColumnDefinitions>
                        <Path Grid.Column="0"
                              Data="M3,6 L10,2 L17,6 L17,16 L3,16 Z M7,16 L7,10 L13,10 L13,16"
                              Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"
                              StrokeThickness="1.4"
                              StrokeLineJoin="Round"
                              StrokeStartLineCap="Round"
                              StrokeEndLineCap="Round"
                              Width="15"
                              Height="15"
                              HorizontalAlignment="Center"
                              VerticalAlignment="Center"
                              Stretch="Uniform"/>
                        <TextBlock x:Name="OpenMainButtonText"
                                   Grid.Column="1"
                                   VerticalAlignment="Center"
                                   Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"
                                   FontSize="12"
                                   FontWeight="SemiBold"/>
                        <Path Grid.Column="2"
                              Data="M4,4 L10,8 L4,12"
                              Stroke="{StaticResource TrayMuted}"
                              StrokeThickness="1.5"
                              StrokeStartLineCap="Round"
                              StrokeEndLineCap="Round"
                              StrokeLineJoin="Round"
                              Width="8"
                              Height="10"
                              HorizontalAlignment="Center"
                              VerticalAlignment="Center"
                              Stretch="Uniform"
                              Opacity="0.75"/>
                    </Grid>
                </Button>

                <Button x:Name="ExitButton"
                        Style="{StaticResource TrayDangerButton}"
                        Margin="0"
                        Click="ExitButton_Click">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="28"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Path Grid.Column="0"
                              Data="M10,2 L10,8 M6.6,4 A6,6 0 1 0 13.4,4"
                              Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"
                              StrokeThickness="1.5"
                              StrokeStartLineCap="Round"
                              StrokeEndLineCap="Round"
                              Width="15"
                              Height="15"
                              HorizontalAlignment="Center"
                              VerticalAlignment="Center"
                              Stretch="Uniform"/>
                        <TextBlock x:Name="ExitButtonText"
                                   Grid.Column="1"
                                   VerticalAlignment="Center"
                                   Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"
                                   FontSize="12"
                                   FontWeight="SemiBold"/>
                    </Grid>
                </Button>
            </StackPanel>

            <TextBlock x:Name="TrayHintText"
                       Grid.Row="3"
                       Foreground="{StaticResource TrayMuted}"
                       FontSize="10.5"
                       Margin="2,8,0,0"/>
        </Grid>
    </Border>
</Window>
